# Claude Subagent Adapters

Status: placeholder

This directory is for Claude-specific subagent definitions.

Source-of-truth role definitions live in:

- `roles/`

Do not create durable policy only in this directory. If a subagent needs a new rule, add it to the relevant `/ai/` source document first, then adapt it here.
