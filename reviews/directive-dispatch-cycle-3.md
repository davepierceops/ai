# Review: skills/directive-dispatch.md — cycle 3

Verdict: ready
Reviewed: `skills/directive-dispatch.md` @ `a7a915cddf3ca1fef774c17942f1e9406cc3715a` (the post-cycle-2 revision pinned by `docs/cycles/trivium-gate-cycle-2-regate-directive.md` @ `80d70d34be5a063b4a28bddc9d1f2a740ee140e2`. Verified by running `git diff --stat a7a915c 80d70d3 -- skills/directive-dispatch.md` → empty, so the reviewed revision is also the current tree state.)
Reviewer: Spec Reviewer Agent (fresh session, Opus 5, Track A; not the instance that executed cycle-2 — `a7a915c` predates this session)
Date: 2026-08-09
Scope: gate re-check of the D3 reversal per `docs/cycles/trivium-gate-cycle-2-regate-directive.md` items 1–6, not a full re-review. Checked: (1) **R1 landed in *Use when*** (`:21-29`) — the bounded exception is gone; the paragraph now reads "The rules here apply to that file too, without exception: a reviewer-gated cycle directive states all four requirements per directive, like any other dispatch," with fresh and Opus 5 named as "the usual selection for that class — the defaults `skills/spec-review-cycle.md` records (Cycle directive format), stated per directive and overridable — not an exemption from stating them"; the pointer resolves, since that section does record both defaults (`skills/spec-review-cycle.md:135-146`) and does carry the fields in its format block (`:107-108`); (2) **R1 landed in *The four requirements*** (`:33-35`) — the escape clause "except where a class fixes a part in advance and says so, which today is the reviewer-gated cycle directive above" is gone, replaced by "An unstated one is a defect. A class may have a usual selection for a part; that is a default to state, not a licence to omit it," which restates the rule with no exception class; (3) **the reversal reached only what it should** — `git show a7a915c --unified=0 -- skills/directive-dispatch.md` returns hunks at the frontmatter, `:23-29`, `:34-35`, and `:325-331` only, so §1 Route (`:37-44`), §2 Model (`:46-55`), §3 Track (`:57-92`), §4 Execution block (`:94-124`), Track B mechanics, and the `bin/dispatch` deferral are untouched, and §1/§2 were confirmed by reading to carry no exception language, as the cycle-2 directive predicted; (4) **the Track-required half is preserved** — §3 Track is unedited, the all-four rule still reaches track, and the cycle-1 clause in the status line is corrected from "the cycle-directive bounded exception" to "the cycle-directive track requirement", which is what D3's surviving half actually installed; (5) **no dangling reference survives here** — `git grep -n -i "bounded exception\|fixed by class\|not restated per directive" -- skills/directive-dispatch.md` returns nothing, and the three remaining "cycle directive" hits (`:26`, `:325`, `:329`) are the defaults pointer and two changelog clauses, the second of which records the carve-out's *removal* rather than asserting it; (6) **B1 stays closed by statement** — with `skills/spec-review-cycle.md` requiring all of route, model, and track and this document admitting no exemption, "an unstated one is a defect" is satisfied without a new exception class, which is the route `reviews/spec-review-cycle-cycle-1.md` B1 called the smaller change; (7) **mirror consistency** — this document, `skills/spec-review-cycle.md`, `LEXICON.md`, and `DEC-000150` all state all-four-stated with fresh and Opus 5 as overridable class defaults, and the pointer direction is coherent: both mirrors name the Cycle directive format section as the recorder of the defaults, and that section points back at both; (8) **regression** — the *Status of this draft* block (`:314-331`) re-read whole: the new clause cites `docs/cycles/trivium-gate-cycle-2-directive.md` (R1), no longer cites a carve-out the file no longer holds, and retains "Nothing here is agreed", consistent with `status: in-review`. Its date, 2026-08-08, is the directive's date rather than the landing date of `a7a915c` (2026-08-09); the file's existing convention does not discriminate between the two, because the cycle-1 clause's date and `bf3f76f`'s coincide, and the clause cites the directive by path, which is the resolvable handle — recorded, no consequence.
Cross-checked: `skills/spec-review-cycle.md` (Cycle directive format; Hard constraints; Procedure step 5), `LEXICON.md` (`Directive`; `Directive file`; `Execution block`), `policies/decision-log-policy.md` — all @ `a7a915c`; `decisions/log.md` `DEC-000110` and `DEC-000150`, whose R2 audit is recorded in full at `reviews/spec-review-cycle-cycle-3.md` (Scope, item 9); `bin/check-frontmatter` by execution (clean, 47 files in scope); `docs/cycles/trivium-gate-cycle-2-directive.md` @ `6c87ea9` (R1, R2, Deferred, Execution notes) and `reviews/directive-dispatch-cycle-1.md` / `reviews/directive-dispatch-cycle-2.md`.
Not inspected: Track B mechanics, the command blocks against `skills/command-blocks.md`'s criteria, the naming schema, and the `bin/dispatch` deferral, beyond confirming the reversal did not reach them — all unedited at `a7a915c` and last gate-reviewed at cycle 2; `bin/cycle-open` and `docs/packages/package-a-spec.md` AC-CO-3, whose skeleton and criterion still carry none of route, model, or track, deferred by decision (`docs/cycles/trivium-gate-cycle-2-directive.md`, Deferred; `DEC-000150`) and confirmed present by reading, not evaluated; the state of PR #54 — `gh` could not reach GitHub from this session (TLS verification failure), so "superseded — held, do not merge" was not checked against the PR.
Findings: none
Prior cycle: `reviews/directive-dispatch-cycle-2.md`
