# Review: policies/remote-write-verification-policy.md — cycle 2

Verdict: ready
Reviewed: `policies/remote-write-verification-policy.md` @ `5e1dc1fb78d4c4a670d5ec749971e7b766d5924f` (the post-cycle-1 revision pinned by `docs/cycles/trivium-gate-cycle-1-regate-directive.md` @ `f7a766a268d63f2509e97b22de0895549be2a66d`; the content edits are `bf3f76fd122f976760a93c9b322873e8e6ce2acc`. Verified by running `git diff --stat 5e1dc1f f7a766a -- policies/remote-write-verification-policy.md` → empty.)
Reviewer: Spec Reviewer Agent (fresh session, Opus 5; not the instance that executed cycle-1 — `bf3f76f` predates this session)
Date: 2026-08-08
Scope: gate re-check per `docs/cycles/trivium-gate-cycle-1-regate-directive.md` @ `f7a766a` — for each cycle-1 finding against this document (B1, B2/N1), whether the directed resolution landed and is correct as written, plus the cross-file resolution this document carries (D7's pointer into `skills/directive-dispatch.md`). Checked: (1) Rule 3 (`:47-57`) — the abbreviation clause is gone, the two provenance sentences survive as "State SHAs read from git. Never invent one," and the contradiction with the `agreed` `policies/document-metadata-policy.md` is dissolved rather than relocated: that policy still contemplates an abbreviated pointer (`:250`) and `bin/aimeta/expedited.py` still normalizes through `git rev-parse`, and nothing in this document now reaches them; (2) **the added pointer resolves and the rule it points at still exists** — `skills/directive-dispatch.md:101-103` carries "never abbreviate a pointer SHA" inside "§4 Execution block — cited by path and SHA", which is the dispatch-block scope the pointer claims for it, so the narrow rule has a home and the blanket one is not silently lost; (3) the new Scope clause (`:70-77`) against the rule it defers to — `skills/directive-dispatch.md`'s "Verification moves to Dave" is present and says what is attributed to it, and the clause correctly characterises Track B's write path as `mv`/`git add`/`git commit` in a local clone; (4) the "Relationship to existing rules" addition (`:91-94`), that its claim of a two-way citation holds — `skills/directive-dispatch.md` cites this policy twice, at `:82` and `:103`, and this document now cites it back; (5) the whole document re-read for a rule left assuming a read-back the Scope clause now admits may be impossible.
Cross-checked: `skills/directive-dispatch.md` (§4 Execution block; Track B mechanics — "Verification moves to Dave", "Commit, not push"), `policies/document-metadata-policy.md` (status `agreed`; "The record"), `skills/spec-review-cycle.md`, `skills/command-blocks.md`, `LEXICON.md` (all @ `5e1dc1f`); `bin/aimeta/expedited.py` and `bin/flip-agreed` by reading, `bin/check-frontmatter --all` by execution (exit 0 over 47 in-scope files); `decisions/log.md` `DEC-000130`; `docs/cycles/trivium-gate-cycle-1-directive.md` @ `d06d12e` (D7, D14) and `reviews/remote-write-verification-policy-cycle-1.md`
Not inspected: the GitHub MCP transport's actual failure behaviour, unchanged this cycle and still taken as reported; whether the three rules were followed in the sessions that produced them; the `wne-crm` incident at `OPEN-ITEMS.md:80-114` beyond confirming the entry still says what this document says it says — the destroyed-file commit is in another repository and was not fetched; the known content-verification gap (`:96-109`) and the `context-sets/base.md` promotion question (`:111-127`), both untouched by any cycle-1 finding and both still open by their own terms; `vendors/**` beyond the placement test cycle 1 already ran; whether the new Scope clause is discoverable by an agent that loads only this policy — the clause is here, which is what the finding asked for, but no agent-context load was exercised.
Findings: none
Prior cycle: `reviews/remote-write-verification-policy-cycle-1.md`
