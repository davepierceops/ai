# Review: LEXICON.md — cycle 1

Verdict: changes-required
Reviewed: `LEXICON.md` @ `81bd2de55e2a7c5838882bb5b7768abc7ccf8238` (the SHA pinned by `docs/cycles/trivium-gate-2026-08-07-directive.md` @ `343878b36b4884893e8e4305d43541959cb2e582`; the local working tree is at that SHA, and `origin/main` has since moved to `92aca81` — the two commits past the pin (`78f0caf`, `c15cf76`) touch neither this document nor any document in scope)
Reviewer: Spec Reviewer Agent (fresh session, Opus 5; not the instance that authored this document — the file's own history predates this session)
Date: 2026-08-07
Scope: the whole document, gate-reviewed per `roles/spec-reviewer-agent.md` (gate review responsibilities) and `policies/agent-review-policy.md` (Spec Reviewer focus). Checked: (1) internal consistency across the three-layers, Sessions, Dispatch, Blocks, Handoff, and Prompt sections; (2) every cross-reference the document makes, resolved at the pinned SHA; (3) every empirical claim the document makes about another document, read against that document at the pinned SHA; (4) consistency of each definition against the four other documents in this gate batch; (5) whether stated open work names what would resolve it.
Cross-checked: `skills/directive-dispatch.md`, `skills/command-blocks.md`, `skills/spec-review-cycle.md`, `policies/remote-write-verification-policy.md` (the four other documents in this gate batch, all @ `81bd2de`); `roles/orchestrator-agent.md`, `roles/coder-agent.md`, `roles/chief-of-staff.md`, `context-sets/collab-workflow.md`, `docs/packages/package-c-change-package.md`, `policies/commit-and-change-control-policy.md`, `policies/document-metadata-policy.md` (@ `81bd2de`); `OPEN-ITEMS.md` entries at `:747`, `:775`, `:794`, `:901`; `decisions/log.md` `DEC-000060`, `DEC-000080`; `bin/aimeta/scope.py` and `bin/check-frontmatter` by execution
Not inspected: conformance of the rest of the tree to these definitions — the document's own Adoption scope (`:14-21`) disclaims it as a precondition, so a tree-wide term sweep is out of gate scope and belongs to a Depth-3 continuity scan; `roles/orchestrator-agent.md`, `roles/coder-agent.md`, `context-sets/collab-workflow.md`, and `docs/packages/package-c-change-package.md` were read only for the specific usages this document cites, not whole; the claim that `directive` and `execute` "keep the senses the corpus already gave them" (`:23-27`) was spot-checked at the two cited sites only, not established across the corpus; whether the six senses of `handoff` (`:26`) were counted correctly — no census was run.
Findings: 2 blocking, 1 non-blocking, 1 observation
Dave should inspect: B1 (LEXICON and `skills/directive-dispatch.md` disagree about whether a sync block precedes a Track B execution block — two canonical documents, no tiebreak in the tree, and the substitute Track B adopted is a proposal `OPEN-ITEMS.md:901` marks "Not analysed"); and separately, `OPEN-ITEMS.md:747` ("`LEXICON.md` frontmatter is unenforced") now reads stale rather than open — verified by running: `policies/document-metadata-policy.md`'s in-scope list names `LEXICON.md`, `bin/aimeta/scope.py` resolves it as matched, and `bin/check-frontmatter --all` exits 0 over 47 files. Not a defect in a document under review, but the item is unstruck.

## B1 — blocking
Claim: The `Sync block` definition asserts that a sync block precedes **every** execution block in a dispatch; `skills/directive-dispatch.md` documents a Track B dispatch that has no sync block at all.
Location: `LEXICON.md:105-106`, against `skills/directive-dispatch.md:114-220` and `:293`
Evidence: Verified by running (`git show 81bd2de:skills/directive-dispatch.md`). Track B mechanics emit exactly two command blocks — the pre-flight `ls ~/Downloads/<stem>*` (`:138`) and the relocate/commit/echo block (`:156-165`) — and neither is a sync block. The same document's status line records the change explicitly: "Track B mechanics rewritten 2026-08-07 (… standalone sync block dropped)" (`:291-293`). What survives of the sync step in Track B is the word "sync" inside `echo "sync, then read and execute <dest-path> @ $(git rev-parse HEAD)"` (`:152`), and `:187-188` requires that echoed line be "one line, no markdown, no backticks" — i.e. plain prose Dave pastes into a session, not a command block, and carrying no remote name. Verified by running `git show 81bd2de:OPEN-ITEMS.md`: the open item at `:901-916`, "Sync as a skill rather than a step inside every directive," proposes exactly the phrasing `sync, then read and execute <path> @ <sha>` and closes "Not analysed."
Consequence: The two documents cannot both be applied. An agent holding this lexicon reads a conformant Track B dispatch as defective and inserts a sync block the skill deliberately removed; a reviewer holding `directive-dispatch` passes a dispatch the lexicon calls incomplete. Both are canonical and neither defers to the other, so there is no tiebreak in the tree. The concrete loss is the named remote: `skills/command-blocks.md:27-30` forbids relying on the `origin` alias precisely because its failure "surfaces as a downstream symptom — missing work, empty results — rather than as an auth error," and a bare "sync" cannot name a remote. That is the failure `directive-dispatch:104-106` itself describes ("a stale clone reporting missing work is evidence about the clone, not the repo").
Fix: Settle which document is right and change the other in the same cycle. Either narrow this definition ("Precedes every Track A execution block; Track B carries the sync step in the echoed dispatch line"), or restore a sync block to the Track B sequence. Whichever way it goes, `OPEN-ITEMS.md:901` needs closing or an explicit deferral, because Track B has already adopted its proposal ahead of the analysis that item says has not happened.
Related: `reviews/directive-dispatch-cycle-1.md` B1 — the same defect stated from the skill's side.

## B2 — blocking
Claim: The citation offered as the warrant for the layer-2 rule states the opposite of what the cited document says.
Location: `LEXICON.md:38-41`
Evidence: Verified by running (`git show 81bd2de:roles/orchestrator-agent.md`). The lexicon reads: "`execute` and `execution` belong to layer 2, always. This matches every existing use in the repo: `skills/spec-review-cycle.md` names Claude Code the execution layer, and `roles/orchestrator-agent.md` describes executing change packages as its work." The second half does not hold. `roles/orchestrator-agent.md:15` reads "Orchestrator does not execute work; it plans and hands off," and `:63` lists "execute change packages (that is Claude Code with the appropriate agent roles)" under what the role does **not** do. No line in that file describes executing change packages as the Orchestrator's work. The first cited use checks out: `skills/spec-review-cycle.md:11-12` names Claude Code the execution layer.
Consequence: An "always" rule in a definitional document rests on a two-item warrant, and one item, checked, says the reverse. A reader who follows the citation — the intended use of a citation here — finds it contradicted, and the rule reads as asserted rather than derived. The underlying claim about the word's *sense* is in fact supported by the cited line (it uses "execute" for layer-2 agent work and assigns it to Claude Code), so the document is giving away a point it wins.
Fix: Restate to what the file says: `roles/orchestrator-agent.md` uses "execute" for layer-2 agent work and assigns it to Claude Code, explicitly not to the Orchestrator. One clause, and the rule's warrant survives intact.

## N1 — non-blocking
Claim: The `Directive` definition requires all four parts stated every time; the cycle-directive format in a document this lexicon governs requires none of route, model, or track, and the tool that generates cycle directives emits none of them.
Location: `LEXICON.md:64-66`, against `skills/spec-review-cycle.md:101-130` and `bin/cycle-open:116`
Evidence: Verified by running. `git show 81bd2de:skills/spec-review-cycle.md`: the Cycle directive format template (`:101-127`) carries Date and Documents in scope and no route, model, or track field, and `:129-130` reads "Required fields: cycle number, documents in scope with SHAs, one decision entry per finding (including rejections). Everything else as needed." `grep -n "Route\|Model\|Track\|Date:" bin/cycle-open` returns a single hit — `:116`, `lines = [heading, "", "Date: %s" % date, "Documents in scope:"]` — so the skeleton generator bakes the omission in. `git show origin/main:docs/cycles/trivium-gate-2026-08-07-directive.md` (`343878b`), the directive under execution: it states Route and Model and no Track.
Consequence: "All four stated every time" is already false for the repo's most-used directive form, and the omission produces no signal — the live directive is missing Track and nothing flagged it, because the tool that would have prompted for it does not know the field exists. A rule stated as invariant that is routinely and silently violated trains readers to treat the section as aspirational, which is the drift this file exists to prevent.
Fix: Either mark reviewer-gated cycle directives as a bounded exception here and say which parts they carry, or hold `skills/spec-review-cycle.md` and `bin/cycle-open` to the four-part rule. This is the same reconciliation `skills/directive-dispatch.md:21-24` already names as open work; naming it here too, or pointing at that sentence, would at least stop the invariant reading as unqualified.
Related: `reviews/spec-review-cycle-cycle-1.md` B1

## O1 — observation
Claim: Two pieces of open work are recorded in prose with no pointer to where they are tracked, though both are in fact tracked.
Location: `LEXICON.md:122-123` ("Known misuse to correct") and `:125-126` ("Naming it is open work")
Evidence: Verified by running `git show 81bd2de:OPEN-ITEMS.md`. The known misuse is tracked at `:775-791` ("`handoff` at `skills/spec-review-cycle.md:57` contradicts the lexicon"), with a disposition and a stated reason it was not fixed in Trivium. The unnamed decision-session handoff is tracked at `:794-`("A handoff into another decision session has no name"). Both entries cite `LEXICON.md` as their source; neither is cited back. Confirmed still-accurate as written: `git show 81bd2de:skills/spec-review-cycle.md | sed -n '57p'` is `### 2. Directive (handoff artifact)`, so the recorded misuse is live, not stale.
Consequence: `roles/spec-reviewer-agent.md` requires that open questions name what would resolve them. These name the question and not the resolution path, so a reader has to already know `OPEN-ITEMS.md` holds them. The cost is small and one-directional — the tracking exists — but it is the kind of one-way link that goes stale silently when the tracked item is closed and the prose is not.
Fix: Add the `OPEN-ITEMS.md` anchors to both lines, or state in the Adoption scope section that open work named in this file is tracked in `OPEN-ITEMS.md` as a standing convention.

## Checks that passed, recorded because a passing check and an unrun check are not the same thing

The schema has no field for this and `OPEN-ITEMS.md:547` item 3 already records that gap; these are listed here rather than as `observation` entries with an unfillable `Consequence`.

- Every path this document cites resolves at `81bd2de` — `policies/commit-and-change-control-policy.md`, `skills/directive-dispatch.md`, `skills/command-blocks.md`, `skills/spec-review-cycle.md`, `roles/orchestrator-agent.md`, `roles/coder-agent.md`, `roles/chief-of-staff.md`, `context-sets/collab-workflow.md`, `docs/packages/package-c-change-package.md`. `.prompts/` (`:131`) is absent from the tree by design: `.gitignore:9` excludes it, which is what the definition it supports asserts ("generated at execution time and not committed").
- All three "established uses" under `Handoff` (`:117-120`) support the definition: `context-sets/collab-workflow.md:44` "## Session handoff", `roles/coder-agent.md:46` "## Handoff", `docs/packages/package-c-change-package.md:226` "## 9. Handoff — debt this package deliberately did not touch".
- Frontmatter validates and the document is enforced: `bin/check-frontmatter LEXICON.md` exits 0, and `bin/aimeta/scope.py` resolves `LEXICON.md` as in-scope against the globs read from `policies/document-metadata-policy.md`.
- No internal contradiction was found among the Sessions, Dispatch, Blocks, and Prompt sections. `Command block` (`:100-103`), `Paste block` (`:96-98`), and `Execution block` (`:76-80`) are mutually consistent and consistent with `skills/command-blocks.md:11-14`.
- The layer-3 prohibition (`:41-42`) is honoured by `skills/command-blocks.md`, which describes blocks as *running*, never as being executed.
