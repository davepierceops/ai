# Review: skills/spec-review-cycle.md — cycle 8

Verdict: ready
Reviewed: `skills/spec-review-cycle.md` @ `4ccfaeb`
Reviewer: self-review (autonomous, this directive)
Date: 2026-08-10
Scope: the C1 edits only — the `Track: <A | B>` field removed from the Cycle
directive format block, `track` removed from the required-fields sentence, and
the all-four paragraph rewritten to all-three. Checked: (1) the three named
requirements against `skills/directive-dispatch.md`'s "The three requirements"
and `LEXICON.md`'s `Directive`, for word-for-word agreement on which three;
(2) the format block against the required-fields sentence, so neither names a
field the other omits; (3) whether the four-versus-three reconciliation
`reviews/spec-review-cycle-cycle-3.md` item (7) recorded still holds — it argued
the block lists three of four because the execution block is not a field of the
directive *file*, and the same argument now yields two of three; (4) the
surrounding defaults paragraph for any clause that depended on track, read whole;
(5) Procedure steps 6–7 against the push obligation added to
`skills/directive-dispatch.md` in cycle 1's fix.
Cross-checked: `skills/directive-dispatch.md` (Purpose; Use when; The three
requirements; Executor obligations), `LEXICON.md` (`Directive`; `Directive file`;
`Sync block`), `decisions/log.md` `DEC-000150` (unmodified; the reversal is
drafted, not written), `reviews/spec-review-cycle-cycle-3.md` items (1), (5),
and (7).
Not inspected: Hard constraints, Reconciliation, the Review artifact schema, and
Procedure steps 1–5 and 8–11 — cleared at cycles 5 and 7, and not reached by C1.
Findings: none

Two notes, neither a finding. **Step 7 still ends "pushes"**, which is where this
document already carried the obligation cycle 1's B1 restored to
`skills/directive-dispatch.md`; the two now agree rather than one covering for
the other. And `DEC-000150`, whose track half this change reverses, is still the
live agreed entry — the reversal exists only as a proposed `DEC-000180` in
`docs/cycles/friction-refactor-2026-08-09-decisions.md` (D21). Until Dave
promotes it, this document and the decision log disagree, deliberately and on
the record.

Prior cycle: `reviews/spec-review-cycle-cycle-7.md`
