# Review: skills/directive-dispatch.md — cycle 2

Verdict: ready
Reviewed: `skills/directive-dispatch.md` @ `5e1dc1fb78d4c4a670d5ec749971e7b766d5924f` (the post-cycle-1 revision pinned by `docs/cycles/trivium-gate-cycle-1-regate-directive.md` @ `f7a766a268d63f2509e97b22de0895549be2a66d`; the content edits are `bf3f76fd122f976760a93c9b322873e8e6ce2acc`. Verified by running `git diff --stat 5e1dc1f f7a766a -- skills/directive-dispatch.md` → empty.)
Reviewer: Spec Reviewer Agent (fresh session, Opus 5; not the instance that executed cycle-1 — `bf3f76f` predates this session)
Date: 2026-08-08
Scope: gate re-check per `docs/cycles/trivium-gate-cycle-1-regate-directive.md` @ `f7a766a` — for each cycle-1 finding against this document (B1, B2, N1, N2, N3), whether the directed resolution landed and is correct as written, plus the cross-file resolutions this document carries (D1, D2, D3, D7's counterpart pointer). Checked: (1) the Track B sync bullet (`:211-218`) against `LEXICON.md`'s narrowed `Sync block`, for agreement on the direction taken *and* on the reason — same-machine, commit-not-push, working-tree-current check rather than a remote fetch — and against `:114-118`, which now splits the sync step by track instead of requiring a block on every dispatch; (2) the shell-termination bullet (`:190-193`) reduced to a pointer, and **the pointer's target resolved by ordinal** — criterion 6 in `skills/command-blocks.md` is the shell-termination criterion after that document gained a seventh, so the citation still names the rule it means and no enumeration survives here to drift; (3) the bounded exception (`:21-29`, `:33-35`) against its two mirrors for identical content, and against `skills/spec-review-cycle.md`'s section names — "Cycle directive format", "§1 Route", "§2 Model" all resolve; (4) the `bin/dispatch` deferral (`:300-312`) for any surviving count and for the two triggers plus expiry condition it now rests on — `git grep -n "seven cycles"` returns no hit in this document; (5) the clone-root bullet (`:194-200`), that it reaches both the relocate and the append block and states the assumption in prose without adding a guard the directive ruled out; (6) the two-failure trigger (`:69-74`) and the Track B conditions (`:61-64`), that the keep-reason and the `DEC-000080` citation are present and that "contention between concurrent sessions" is now among the conditions the track addresses; (7) the pre-flight prose (`:159-161`) declaring its listing the exempt in-the-moment kind, as `skills/command-blocks.md`'s scoped evidence rule requires the defining document to do; (8) the abbreviation rule at `:101-103`, that it still stands where `policies/remote-write-verification-policy.md` now points at it, and that its scope is dispatch blocks.
Cross-checked: `skills/command-blocks.md` (criterion 6 by ordinal, the scoped evidence rule, the rewritten sync/remote rule), `LEXICON.md` (`Sync block`, `Directive`), `skills/spec-review-cycle.md` (Cycle directive format), `policies/remote-write-verification-policy.md` (Rule 3 and the new Scope clause, both directions of the pointer) — all @ `5e1dc1f`; `decisions/log.md` `DEC-000080`, `DEC-000090`, `DEC-000100`, `DEC-000110`, `DEC-000130`; `OPEN-ITEMS.md:907` (the entry D1 closed); `bin/cycle-open --help` by execution; `docs/cycles/trivium-gate-cycle-1-directive.md` @ `d06d12e` (D1, D2, D3, D9, D10, D11) and `reviews/directive-dispatch-cycle-1.md`
Not inspected: whether the Track B sequence runs end to end as documented — no clone-and-rehearse was performed this cycle either, and the D10 resolution is prose, so the wrong-cwd case remains stated rather than guarded, by decision; the model-selection table's cost/quality claims (`:46-55`), untouched this cycle and still judgment calls the document labels "deliberately crude"; the claim that "Track B has now been run once (2026-08-07)" (`:308-309`), untouched and still without a located artifact; the "reconciling the rest of the duplication is open work" clause at `:28-29`, which the cycle-1 findings did not reach and which this cycle narrows but does not close — it names no tracking anchor, and no `OPEN-ITEMS.md` entry was searched for it; the directive-file naming schema (`:277-298`), untouched this cycle and still labelled a proposal.
Findings: none
Prior cycle: `reviews/directive-dispatch-cycle-1.md`
