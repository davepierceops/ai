# Review: LEXICON.md — cycle 3

Verdict: ready
Reviewed: `LEXICON.md` @ `a7a915cddf3ca1fef774c17942f1e9406cc3715a` (the post-cycle-2 revision pinned by `docs/cycles/trivium-gate-cycle-2-regate-directive.md` @ `80d70d34be5a063b4a28bddc9d1f2a740ee140e2`. Verified by running `git diff --stat a7a915c 80d70d3 -- LEXICON.md` → empty, so the reviewed revision is also the current tree state.)
Reviewer: Spec Reviewer Agent (fresh session, Opus 5, Track A; not the instance that executed cycle-2 — `a7a915c` predates this session)
Date: 2026-08-09
Scope: gate re-check of the D3 reversal per `docs/cycles/trivium-gate-cycle-2-regate-directive.md` items 1–6, not a full re-review. Checked: (1) **R1 landed** in the `Directive` definition (`:65-71`) — the "*Bounded exception:* a reviewer-gated cycle directive states track and the execution block, and takes route (fresh) and model (Opus 5) as fixed by its class" sentence is gone, replaced by "No class is exempt from stating a part. A class may have defaults: a reviewer-gated cycle directive defaults to route *fresh* and model *Opus 5*, states them like any other dispatch, and may override them"; the pointer resolves — `skills/spec-review-cycle.md`'s Cycle directive format section does record both defaults (`:135-146`) and does carry the fields (`:107-108`); (2) **the Track-required half is preserved** — the four parts are still enumerated with track among them (`:66-67`), so D3's surviving half is intact here; (3) **the reversal reached only what it should** — `git show a7a915c --unified=0 -- LEXICON.md` returns hunks at the frontmatter and `:67-71` only, and the neighbouring definitions were re-read for a dependency on the removed sentence: `Dispatch`, `Track A / Track B`, `Execution block`, `Directive file`, `Instruction`, `Companion document`, and `Sync block` carry none, nor does the three-layers section (`:29-43`); (4) **no dangling reference survives here** — `git grep -n -i "bounded exception\|fixed by class\|not restated per directive" -- LEXICON.md` returns one hit, `:68`, which is the new sentence denying the exemption rather than asserting it; (5) **B1 / N1 stay closed by statement** — `reviews/LEXICON-cycle-1.md` N1's claim was that this definition's "all four stated every time" was already false for the repo's most-used directive form; with `skills/spec-review-cycle.md` now requiring route, model, and track and `skills/directive-dispatch.md:33-35` admitting no exemption, the invariant is true of the documents rather than qualified by an exception class, which is the second of the two fixes N1 named and the one B1's counterpart called smaller; (6) **mirror consistency** — this definition, `skills/spec-review-cycle.md:135-146`, `skills/directive-dispatch.md:21-35`, and `decisions/log.md` `DEC-000150` all state all-four-stated with fresh and Opus 5 as overridable class defaults, and no document still asserts fixity; (7) **regression** — the override permission read against `skills/spec-review-cycle.md`'s Hard constraints: this file states the override at the definitional layer and grounds the default in no hard constraint of its own (`git grep -n "Hard constraints" -- LEXICON.md` → nothing), so the seam recorded at `reviews/spec-review-cycle-cycle-3.md` N1 belongs to that document and not to this one; (8) the Adoption scope and touch rule (`:14-21`) re-read and unaffected by the edit.
Cross-checked: `skills/spec-review-cycle.md` (Cycle directive format; Hard constraints; Procedure step 5), `skills/directive-dispatch.md` (Use when; The four requirements; §1 Route; §2 Model; §4 Execution block), `policies/decision-log-policy.md` — all @ `a7a915c`; `decisions/log.md` `DEC-000110` and `DEC-000150`, whose R2 audit is recorded in full at `reviews/spec-review-cycle-cycle-3.md` (Scope, item 9); `bin/check-frontmatter` by execution (clean, 47 files in scope); `docs/cycles/trivium-gate-cycle-2-directive.md` @ `6c87ea9` (R1, R2) and `reviews/LEXICON-cycle-1.md` N1 / `reviews/LEXICON-cycle-2.md`.
Not inspected: every definition outside the Dispatch section beyond confirming the edit did not reach it — unedited at `a7a915c` and last gate-reviewed at cycle 2; tree-wide conformance to the lexicon, which the Adoption scope (`:14-21`) explicitly excludes as a precondition and which the touch rule spreads with the work instead.
Findings: none
Prior cycle: `reviews/LEXICON-cycle-2.md`
