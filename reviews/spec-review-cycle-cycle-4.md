# Review: skills/spec-review-cycle.md — cycle 4

Verdict: ready
Reviewed: `skills/spec-review-cycle.md` @ `2fe299cade4325e80a3d51d3e2513c970f921b20` (the N1 fix, pinned by `docs/cycles/spec-review-cycle-cycle4-regate-directive.md` @ `d707d906116be8870decbf68f34794f8ce52465a`. Verified by running `git diff --stat 2fe299c HEAD -- skills/spec-review-cycle.md` → empty, so the reviewed revision is also the current tree state at `c6eb1b2`.)
Reviewer: Spec Reviewer Agent (fresh session, Opus 5, Track A; not the instance that executed the N1 fix — `2fe299c` predates this session). The reflexivity holds a fourth time: this document specifies the schema this artifact is written in and the filename it is named by.
Date: 2026-08-09
Scope: gate re-check per `docs/cycles/spec-review-cycle-cycle4-regate-directive.md` items 1–4 — a confirmation pass, not a full re-review. Checked: (1) **N1 resolved** — `git show 2fe299c` is a five-line insertion after "…and the stated field governs" (`:145`) and nothing else, reading "What route selects is the **execution** session (`LEXICON.md`, the three layers): `existing context` names an already-running *execution* session, and never releases the Hard constraints above or step 5's prohibition on continuing into execution in the conversation that produced the directive — whatever route is stated" (`:145-149`). The forbid/license seam is gone: precedence is now stated, in the direction the Hard constraints require, and stated as a limit on what the value *means* rather than on the reader's inference from a section title. Both citations resolve and are characterized accurately — `:27-39` is `## Hard constraints`, and Procedure item 5 (`:61-62`) is "The cycle chat is done. Do not continue into execution in the same conversation," which the clause paraphrases without widening. The `LEXICON.md` pointer resolves: `## The three layers` (`:29-43`) assigns `execute`/`execution` to layer 2 "always" and layer 1 to chat, which is exactly the distinction the clause turns on, and `## Sessions` (`:45-61`) supplies the term of art it uses. (2) **Route stays overridable** — `git diff --stat a7a915c HEAD -- decisions/log.md` → empty, so `DEC-000150` is untouched, and the clause leaves `existing context` a non-empty case rather than re-fixing route by class under another name: `skills/directive-dispatch.md:43-44` admits it "only when the work depends on state the running session holds and the directive cannot carry," a condition satisfiable at layer 2 (a prior execution session mid-work on the same branch, or one resumed after step 8 stopped it). "These are defaults, not fixities … the stated field governs" (`:143-145`) survives verbatim. (3) **No new seam** — the whole edited paragraph (`:135-150`) re-read against the Hard constraints and step 5, plus four adversarial readings: *(a)* "the stated field governs" immediately followed by a limit on its domain is a narrowing, not a contradiction — the field governs, and the next sentence says what it selects; *(b)* Hard constraint 1's "Never carry forward chat history" (`:29-32`) is a claim about chat, and an already-running execution session holds working-tree and layer-2 state, not chat history, so the clause admits nothing the constraint forbids; *(c)* the pre-existing grounding of the fresh default in "one conversation per cycle" (`:139-141`) — a decision-layer rule cited as reason for a field that selects an execution session — is a rationale rather than a rule, is joined to the route-relevant half ("execution starts a session that holds none of it") by "and", is unchanged by this fix, and is now foreclosed by the very next sentence, so the clause narrows that ambiguity rather than adding one; *(d)* "The same framing is mirrored at `skills/directive-dispatch.md` (Use when) and `LEXICON.md` (`Directive`)" (`:149-150`) does not become an over-claim — the mirrorable half is mirrored (`LEXICON.md:65-71` makes the directive "the complete package handed to an **execution session**" with route among its four parts and its values "fresh **session** or existing context"; `skills/directive-dispatch.md:17-29` frames the move as decision session → execution session and §1 Route (`:37-44`) speaks of "the running session"), while the precedence half concerns Hard constraints that exist only in this document — `git grep -n -i "hard constraint" -- skills/directive-dispatch.md LEXICON.md` returns nothing, so there is nothing there to mirror and no reading on which the sentence asserts the mirrors restate a constraint they do not carry. The Fallback (`:96-99`) was also re-read: it runs with no execution session at all, so the route field is unused there rather than falsified — a pre-existing condition the clause neither creates nor misdescribes. (4) **Scope held** — `git show --stat 2fe299c` touches one file, `skills/spec-review-cycle.md`, 5 insertions / 1 deletion; `git diff --stat a7a915c HEAD -- skills/directive-dispatch.md LEXICON.md` → empty, so neither mirror was edited; frontmatter is unchanged at `status: in-review`, `last-reviewed: null`; `git diff --stat a7a915c HEAD` over the whole tree adds only this fix, three cycle directives, and the three cycle-3 artifacts. `bin/check-frontmatter` runs clean (47 files in scope, exit 0).
Cross-checked: `skills/directive-dispatch.md` (Use when; The four requirements; §1 Route; §2 Model), `LEXICON.md` (The three layers; Sessions; `Directive`; `Directive file`; `Execution block`) — both @ `c6eb1b2`, verified unedited since `a7a915c`; `decisions/log.md` `DEC-000150`; `docs/cycles/spec-review-cycle-n1-fix-2026-08-09-directive.md` (Change, Scope / do-not, Executor obligations) against `2fe299c`'s diff and commit message; `reviews/spec-review-cycle-cycle-3.md` N1 (Fix) against the clause as landed; `bin/check-frontmatter` by execution.
Not inspected: everything outside the N1 clause and the sections it cites — this is a confirmation pass, and the rest of the document was reviewed at cycle 3 and is byte-identical to `a7a915c` outside the five inserted lines; the chat-side procedure (steps 1–3 and 9–11) as practised, unobservable from the repository as in cycles 1–3; `bin/cycle-open`'s `render_directive` (`:115-132`) and `docs/packages/package-a-spec.md` AC-CO-3, which still emit and enumerate none of route, model, or track — confirmed still present by reading, not evaluated, deferred by decision (`docs/cycles/trivium-gate-cycle-2-directive.md`, Deferred / out of scope; `DEC-000150` "Consequence for tooling"); the state of PR #54, not re-checked this cycle; the review-artifact schema's shape for a check that passed, still deferred (`OPEN-ITEMS.md:547`), so this artifact again carries such checks in `Scope`; `skills/directive-dispatch.md` and `LEXICON.md` as documents, already cycle-3 `ready` and out of scope here.
Findings: none
Prior cycle: `reviews/spec-review-cycle-cycle-3.md`
