# Review: skills/command-blocks.md — cycle 2

Verdict: ready
Reviewed: `skills/command-blocks.md` @ `5e1dc1fb78d4c4a670d5ec749971e7b766d5924f` (the post-cycle-1 revision pinned by `docs/cycles/trivium-gate-cycle-1-regate-directive.md` @ `f7a766a268d63f2509e97b22de0895549be2a66d`; the content edits are `bf3f76fd122f976760a93c9b322873e8e6ce2acc`. Verified by running `git diff --stat 5e1dc1f f7a766a -- skills/command-blocks.md` → empty.)
Reviewer: Spec Reviewer Agent (fresh session, Opus 5; not the instance that executed cycle-1 — `bf3f76f` predates this session)
Date: 2026-08-08
Scope: gate re-check per `docs/cycles/trivium-gate-cycle-1-regate-directive.md` @ `f7a766a` — for each cycle-1 finding against this document (B1, B2, N1), whether the directed resolution landed and is correct as written, plus the ordinal check the re-gate directive requires of D5's added seventh criterion. Checked: (1) the shell-termination rule (`:64-74`) and criterion 6 (`:90-92`) restated by effect with `set -e` named, and the "known instance, not the rule" pattern now matching the copy-control rule's, so the document holds one pattern rather than two; (2) **the criteria enumerated by ordinal** — `awk`/`grep` over `:76-98` returns exactly seven bullets, criterion 6 is the shell-termination rule and criterion 7 the new sync/remote rule, so `skills/directive-dispatch.md`'s citation of criterion 6 still resolves to the rule it means and the count sentence at `:78` ("all seven") is correct; (3) the appended-not-slotted note (`:96-98`), which is what makes the criteria order legitimately diverge from body order — the rewritten sync rule sits fourth in the body and seventh in the list; (4) the rewritten remote rule (`:35-43`) against criterion 7 for agreement on both halves (remote *and* ref; exit status checked), and the removed false rationale; (5) the scoped definition of *evidence* (`:28-33`) against criterion 4 and against the pre-flight `ls` it exempts, read at its definition site in `skills/directive-dispatch.md`; (6) the tree re-grepped for blocks the rewritten rule and the new criterion would newly fail.
Cross-checked: `skills/directive-dispatch.md` (Track B pre-flight, relocate, and append blocks — still the tree's only published command blocks; and its criterion-6 pointer), `LEXICON.md` (`Command block`, `Paste block`, `Sync block`), `skills/spec-review-cycle.md`, `policies/remote-write-verification-policy.md` (all @ `5e1dc1f`); `decisions/log.md` `DEC-000100`, `DEC-000120`; `docs/cycles/trivium-gate-cycle-1-directive.md` @ `d06d12e` (D2, D5, D8) and `reviews/command-blocks-cycle-1.md`
Not inspected: `set -e`'s interactive-shell behaviour was not re-executed this cycle — cycle-1 verified it by running under both `bash -i` and `zsh -i`, and the edit under review changes the document, not the behaviour; the heredoc/copy-control claim at `:52-55`, unchanged this cycle and still a rendering property of the Claude desktop client that cannot be checked from here; no tree-wide command-block conformance sweep was run against the new criterion 7 — `git grep -nE '^\s*git (fetch|pull|push)' -- '*.md'` returns nothing, which bounds live exposure to zero but is not a sweep, and criterion 7 therefore has no exemplar in the tree to check it against (the Track A sync block appears only as the placeholder `<sync block>`); the `name:`/`description:` frontmatter dialect question, still tracked at `OPEN-ITEMS.md:851` and still out of this gate's scope; the skill's behaviour when loaded by Claude Code as a skill (not exercised).
Findings: none
Prior cycle: `reviews/command-blocks-cycle-1.md`
